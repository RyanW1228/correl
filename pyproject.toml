[project]
name = "correl"
version = "0.0.1"
description = "Correl prototype: equivalence groups + redemption invariant"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
